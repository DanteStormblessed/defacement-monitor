apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: target-manager
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: target-manager
                port:
                  number: 80
